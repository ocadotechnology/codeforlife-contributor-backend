name: Main

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  main:
    uses: ocadotechnology/codeforlife-workspace/.github/workflows/backend.yaml@contributor-backend-49 # TODO: set to @main
    secrets: inherit
